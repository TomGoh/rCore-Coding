[package]
name = "easy-fs"
version = "0.1.0"
edition = "2024"

[dependencies]
spin = "0.7.0"
lazy_static = { version = "1.4.0", features = ["spin_no_std"] }
lru = "0.16.2"

[profile.release]
debug = true

[features]
board_qemu = []
board_k210 = []
